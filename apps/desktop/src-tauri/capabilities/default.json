{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core window capability with no filesystem or shell plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
